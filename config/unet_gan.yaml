base_kwargs:
  device: gpu
  name: UNetGANUpscaler2
  save_dir: /home/users/l/leighm/scratch/Saved_Networks/ImageProcessing
  net_type: unet-gan

unet_kwargs:
  min_size: 4
  attn_after: 3
  start_channels: 16
  max_channels: 128
  resnet_kwargs:
    dims: 2
    act: lrlu
    drp: 0.1
    nrm_groups: 4
  attn_kwargs:
    num_heads: 4
    nrm_groups: 4

disc_kwargs:
  outp_size: 64
  start_channels: 8
  max_channels: 32
  resnet_kwargs:
    dims: 2
    act: lrlu
    drp: 0.1
    nrm_groups: 8

optim_dict:
  name: adam
  lr: 5.0e-4

sched_dict:
  name: warmup
  num_steps: 512
