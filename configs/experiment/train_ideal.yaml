# @package _global_

defaults:
  - override /datamodule: cifar10.yaml
  # - override /datamodule: animals.yaml
  - override /model: ideal.yaml

datamodule:
  loader_config:
    batch_size: 256
    num_workers: 8

# loggers:
#   wandb:
#     offline: True

trainer:
  precision: 32
  # strategy: ddp_find_unused_parameters_true # For ddp to allow ema networks
  accelerator: auto
  devices: auto

# For faster setup
precision: high
compile: null

# Required for image loading
resize: 80
crop_size: 64

project_name: image_processing
network_name: cifar10_ideal
