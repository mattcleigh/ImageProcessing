# @package _global_

defaults:
  - override /datamodule: cifar10.yaml
  # - override /datamodule: animals.yaml
  - override /model: ct.yaml

datamodule:
  loader_config:
    batch_size: 256
    num_workers: 8

# loggers:
#   wandb:
#     offline: True

trainer:
  precision: 16-mixed
  # strategy: ddp_find_unused_parameters_true # For ddp to allow ema networks
  accelerator: gpu
  devices: auto

# For faster setup
precision: medium
compile: null

project_name: image_processing
network_name: cifar10_ct
